import {
  LayoutDashboard,
  Swords,
  Users,
  TestTubes,
  FileText,
  Settings,
} from 'lucide-react';

export const IconDashboard = LayoutDashboard;
export const IconMatches = Swords; // Crossed swords for matches
export const IconPlayers = Users;
export const IconSimulation = TestTubes; // Lab tubes for simulation
export const IconReports = FileText;
export const IconSettings = Settings;

